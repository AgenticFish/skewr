name: skewr_cli
description: A CLI tool for skewr.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.7.0
  chat_adapter:
    path: ../chat_adapter
  chat_core:
    path: ../chat_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
